export interface ProjectInfo {
  title: string;
  description: string;
  contributors: string[];
  links: string[];
}

export interface HeaderText {
  heading: string;
  p_text: string;
}

export interface AbstractCardInfo {
  title: string;
  desc: string;
  img_link: string;
  router_link: string;
}
